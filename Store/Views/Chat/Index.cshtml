@* @using Store.Chat.Models
@model List<Conversation>

<h2>Conversations</h2>
<ul>
    @foreach (var conv in Model)
    {
        <li>
            <a href="@Url.Action("Chat", "Chat", new { id = conv.Id })">
                Conversation @conv.Id
            </a>
        </li>
    }
</ul> *@